# Backups

Esta carpeta guarda respaldos versionados del estado del proyecto.

Convencion:

- una carpeta por fecha en formato `YYYY-MM-DD`
- cada respaldo incluye un resumen corto del estado alcanzado
- la rama `backup` contiene el snapshot completo del proyecto en la fecha registrada
