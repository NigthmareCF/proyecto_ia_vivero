package com.vivero.shared.enums;

/**
 * Tipos de comando aceptados por el módulo robot.
 */
public enum RobotCommandType {
    START_PATROL,
    STOP_PATROL,
    GOTO_PLANT,
    SET_MODE,
    MANUAL_MOVE,
    HEARTBEAT,
    SWITCH_CAMERA,
    SET_SPEED_PROFILE,
    SET_STREAM_PROFILE,
    RUN_ACRO
}
