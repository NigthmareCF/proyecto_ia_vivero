package com.vivero.shared.enums;

/**
 * Canales disponibles para enviar notificaciones y reportes al usuario.
 * El usuario puede activar uno o varios simultáneamente desde la interfaz.
 */
public enum NotificationChannel {
    EMAIL
}
